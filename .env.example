# GraphQL API Endpoints
# When using Vite proxy (recommended for development), use relative paths:
VITE_GRAPHQL_HTTP_ENDPOINT=/api/graphql
VITE_GRAPHQL_WS_ENDPOINT=/socket

# For direct backend connection (requires CORS configuration in backend):
# VITE_GRAPHQL_HTTP_ENDPOINT=http://localhost:4000/api/graphql
# VITE_GRAPHQL_WS_ENDPOINT=ws://localhost:4000/socket
